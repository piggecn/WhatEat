# 本次更新（v0.0.4）

## 新增

- **菜谱导入三大来源**
  - 🥘 **HowToCook 中文菜谱库**（免 Key，约 390 道家常菜，MIT 协议）：中文关键词直接搜索，支持「番茄↔西红柿」等同义词，自动拆出食材用量与步骤
  - 🔗 **网页链接导入**：粘贴菜谱网页链接自动解析（支持豆果美食、含 JSON-LD 的菜谱站；带 SSRF 防护，仅抓取你指定的单页）
  - 📋 **粘贴导入**：小红书/公众号等复制文字一键拆分标题、食材、步骤

- **离线菜谱索引（1281 道菜）**：TheMealDB 790 + HowToCook 368 + 维基教科书食谱 123 道全部打包进镜像，搜索和导入不再依赖外网；索引缺失时自动退回在线模式

- **TTS 多音色朗读**：烹饪模式支持 5 种 Edge 高质量音色（晓晓/晓伊/云希/云扬/云健）+ 本机语音，可调语速/音调，自动朗读开关

- **烹饪模式**：全屏大字步骤 + 计时器 + 上一步/下一步 + 语音朗读

- **日历升级**：每餐支持多道菜、本月回顾统计（已安排 X 餐 + 最常做 TOP5）、购物清单按天/汇总切换

- **家里剩啥**：库存独立页面（新增/删除食材），随机推荐支持「仅用现有食材」

- **OCR 拍照导入**：拍照识别菜谱文字直接转表单（RapidOCR 中文识别）

- **其他**：搜索历史与关键词高亮、单位换算（克↔两、毫升↔汤匙）、时令推荐提示、深浅色主题、食谱照片 OCR 导入

## 修复

- 搜索关键词高亮不显示（模板变量名错误）
- 详情页忌口标签不显示（json 模块未导入）
- 容器内访问 TheMealDB/图片搜索全部失败（代理地址指向容器自身，已自动归一化为宿主机）
- 搜索后输入框被清空、搜索图标跑出输入框（Alpine 初始化覆盖 + 定位容器错误）
- 豆果链接导入的步骤混入「烹饪技巧」无关内容
- HowToCook 部分菜谱（如拔丝土豆）食材解析为空（补充「名称 数量 单位」格式）

## 优化

- **镜像瘦身 1.12GB → 750MB**：OpenCV 换 headless 版（省去 libGL 系统库）、移除 uvloop 等冗余依赖、构建禁用字节码
- 排除 Flutter 移动端目录进镜像（曾导致镜像膨胀到 17GB）
- 静态资源按版本号打戳，彻底解决浏览器缓存旧页面

## 其他

- 移除点赞+评论功能（按需求）
- 库存入口改为 header 导航「家里剩啥」
